<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twoc15.traity.musicfeelings" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CreateTag"
            android:label="@string/title_activity_create_tag" >
        </activity>
        <activity
            android:name=".ModifyTag"
            android:label="@string/title_activity_modify_tag" >
        </activity>
        <activity
            android:name=".AudioPlay"
            android:label="@string/title_activity_audio_play" >
        </activity>
    </application>

</manifest>
